[package]
name = "openlogi-fixture"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
description = "Host-free schemas and privacy verification for OpenLogi device fixtures."
keywords = ["logitech", "hidpp", "fixture", "testing"]
categories = ["development-tools::testing", "hardware-support"]

# Fixture contracts must remain usable by portable replay and published CLI
# tooling. This crate performs no file I/O, host access, or async work.
[dependencies]
openlogi-core = { version = "0.8.5", path = "../openlogi-core", default-features = false }
serde = { workspace = true }
serde_ignored = "0.1.14"
thiserror = { workspace = true }

[dev-dependencies]
serde_json.workspace = true

[lints]
workspace = true
